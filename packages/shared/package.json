{
  "name": "shared",
  "version": "1.0.0",
  "scripts": {
    "prisma:generate": "npx prisma generate",
    "build": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2"
  },
  "devDependencies": {
    "prisma": "^5.10.2",
    "@types/node": "^22.10.7"
  }
}